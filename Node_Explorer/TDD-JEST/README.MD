```bash
> npm init 

#Esse comando serve para criação de um package.json para o seu projeto.
# Apertando Enter em cada uma das perguntas que ele lhe fará no console usa a configuração padrão. 
# Atente apenas ao fato de que quando chegar a pergunta 'test command', digite 'jest'

#Install Jest
> npm install --save-dev jest

#Só funcionou o jest --init após instalação global
> npm install -g jest
> jest --init



```